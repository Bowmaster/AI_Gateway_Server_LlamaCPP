# AI Lab Server Dependencies
# Install with: pip install -r requirements.txt

# Core server framework
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0

# HTTP client (for llama-server communication and streaming)
requests>=2.28.0
httpx>=0.24.0

# Process management and system info
psutil>=5.9.0

# Model downloads from HuggingFace
huggingface_hub>=0.16.0

# GPU detection (optional, for hardware profiling)
nvidia-ml-py>=12.0.0

# Web search and scraping tools (for server-side tools)
ddgs>=0.1.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
